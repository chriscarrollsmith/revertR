% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculator.R
\name{clean_data}
\alias{clean_data}
\title{Data Cleaning for Financial Time Series}
\usage{
clean_data(df, min_points = 1021)
}
\arguments{
\item{df}{A data frame requiring cleaning. It must contain at least the columns 'symbol' and 'adjusted'.
'symbol' represents the stock ticker, and 'adjusted' represents the adjusted closing prices of the stock.}
}
\value{
A cleaned data frame where each 'symbol' has been grouped and a new column 'n' added to
indicate the number of observations for that 'symbol'. Symbols with less than 1022 observations
or with more than one 'NA' in the 'adjusted' column have been removed. Individual rows with 'NA'
in the 'adjusted' column are also removed.
}
\description{
This function provides a specialized cleaning routine tailored for financial time series data.
It performs the following operations on the input data frame:
\enumerate{
\item Counts the number of observations for each ticker ('symbol').
\item Filters out tickers with an insufficient number of observations (less than 1022).
\item Filters out tickers that have more than one 'NA' in the 'adjusted' price column.
\item Removes individual rows with 'NA' in the 'adjusted' price column.
}
}
\details{
The cleaned data is expected to provide a more consistent and reliable dataset for financial analysis.
}
\examples{
\dontrun{
data <- data.frame(
  symbol = c(rep("AAPL", 5), rep("MSFT", 5)),
  adjusted = c(145, 146, NA, 148, 149, 220, 221, NA, 223, NA)
)
clean_data(data)
}

}
